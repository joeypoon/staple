var username = '<%= j @user.username %>';
var recentStaples = '<%= j render 'users/show_user' %>'

$('#modal .modal-title').html(username);
$('#modal .modal-body').html(recentStaples);

$('#modal').modal('show');
