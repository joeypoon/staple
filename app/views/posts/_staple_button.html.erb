<% unless current_user.stapled.include? post.id %>
  <%= link_to staple_post_path(post_id: post.id), remote: true do %>
    <button class="btn btn-success">Staple</button>
  <% end %>
<% else %>
  <button class="btn btn-danger">Unstaple</button>
<% end %>
